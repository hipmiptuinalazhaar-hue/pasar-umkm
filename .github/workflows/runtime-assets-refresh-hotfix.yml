name: Runtime Assets Refresh Hotfix

on:
  push:
    branches:
      - fix/chat-header-freeze-v9

permissions:
  contents: write

jobs:
  refresh-runtime-assets:
    uses: ./.github/workflows/runtime-assets.yml
    with:
      write_changes: true
    permissions:
      contents: write
